apiVersion: v1
kind: Service
metadata:
  name: tiny-service
  namespace: monitoring
  labels:
    app: tiny-service
spec:
  selector:
    app: tiny-service
  ports:
    - name: http
      protocol: TCP
      port: 5000
      targetPort: 5000
  type: ClusterIP